{
  "name": "@trace-ux/tracker",
  "version": "0.1.0",
  "private": true,
  "description": "TraceUX tracker snippet: records session replay events and ships them to a TraceUX server",
  "type": "module",
  "scripts": {
    "build": "esbuild src/main.ts --bundle --minify --format=iife --target=es2018 --legal-comments=none --outfile=dist/tracker.js",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "esbuild": "^0.25.0",
    "typescript": "^5.6.0"
  },
  "dependencies": {
    "@rrweb/record": "^2.1.1"
  }
}
